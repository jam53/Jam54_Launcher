using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UIElements;
using UnityEngine.UIElements.Experimental;

//This script runs only once, when the progam just launcher, and loads in all the titles, images etc. into the placeholder objects.
//So the text 'Title Game ription' inside a button would be changed to 'AstroRun' for example

public class InitialSetup : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
